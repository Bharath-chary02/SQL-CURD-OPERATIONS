<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Delete User</title>
  <style>
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: Arial, sans-serif; }

    body {
      background: #F1EFEC;
      display: flex;
      justify-content: center;
      align-items: center;
      min-height: 100vh;
      padding: 20px;
      color: #030303;
    }

    .container {
      background: #D4C9BE;
      padding: 40px;
      border-radius: 12px;
      box-shadow: 0 6px 18px rgba(3,3,3,0.08);
      max-width: 500px;
      width: 100%;
    }

    h1 { 
      margin-bottom: 24px; 
      text-align: center; 
      color: #8A5A2B;
    }

    label { display: block; margin: 12px 0 6px; font-weight: bold; }
    input { 
      width: 100%; 
      padding: 10px; 
      border-radius: 8px; 
      border: 1px solid rgba(0,0,0,0.1); 
      margin-bottom: 20px;
    }

    input:disabled {
      background-color: #e0d8ce;
      color: #555;
    }

    .btn {
      display: inline-block;
      padding: 12px 20px;
      border-radius: 8px;
      background: #8A5A2B;
      color: white;
      font-weight: bold;
      cursor: pointer;
      border: none;
      transition: transform 0.1s ease, box-shadow 0.1s ease;
      width: 100%;
      text-align: center;
    }

    .btn:hover { 
      transform: translateY(-2px); 
      box-shadow: 0 6px 18px rgba(3,3,3,0.1); 
    }

    .cancel-btn {
      background: #ff4c4c;
      margin-top: 10px;
    }

    @media screen and (max-width: 480px) {
      .container { padding: 30px; }
      input, .btn { font-size: 0.95rem; }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Delete User</h1>
    <form method="post" action="/user/<%= user.userId %>/delete?_method=DELETE">
      <label for="email">Email:</label>
      <input type="email" name="useremail" id="email" placeholder="<%= user.email %>" disabled>

      <label for="password">Password:</label>
      <input type="password" name="password" id="password" placeholder="Enter your password" required>

      <button type="submit" class="btn">Delete Account</button>
      <a href="/user" class="btn cancel-btn">Cancel</a>
    </form>
  </div>
</body>
</html>
